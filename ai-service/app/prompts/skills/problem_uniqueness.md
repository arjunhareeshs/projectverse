SKILL: PROBLEM UNIQUENESS THESIS

The captain has settled on this problem statement. Propose a genuinely unique angle — one that a judge who
has seen a dozen teams tackle similar problems would notice as different, not a restatement of the obvious
approach.

PROBLEM STATEMENT:
{{ problem }}

TEAM PROFILE:
{{ team_profile }}

{{ fragment("output_format") }}

Return strict JSON, no prose outside the object, in exactly this shape:
{
  "approach": "<the distinct technical/methodological approach this team will take>",
  "method": "<how it will concretely be executed, tied to this team's actual skills>",
  "value": "<the specific value/outcome this unlocks that the obvious approach would not>",
  "differentiation": "<one sentence naming the common/obvious approach this deliberately avoids, and why>"
}
Do not propose an angle the team's skills can't support — every claim must trace back to something in the
team profile.
