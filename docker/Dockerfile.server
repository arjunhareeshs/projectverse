FROM node:20-alpine
WORKDIR /app
COPY package.json pnpm-workspace.yaml ./
COPY server/package.json ./server/package.json
RUN corepack enable && corepack prepare pnpm@10.14.0 --activate && pnpm install --frozen-lockfile=false
COPY . .
WORKDIR /app/server
EXPOSE 4000
CMD ["pnpm", "dev"]
